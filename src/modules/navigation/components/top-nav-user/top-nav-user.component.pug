li.nav-item.dropdown.dropdown-user.no-caret(
    *ngIf='userService.user$ | async as user',
    ngbDropdown,
    placement='bottom-right',
    display='dynamic'
)
    a#userDropdown.nav-link.dropdown-toggle(
        ngbDropdownToggle,
        data-cy='userMenu',
        role='button',
        aria-haspopup='true',
        aria-expanded='false'
    )
        fa-icon([icon]='["fas", "user"]')
    .dropdown-menu.dropdown-menu-right(
        ngbDropdownMenu,
        aria-labelledby='dropdownUser'
    )
        h6.dropdown-header
            .dropdown-user-details
                .dropdown-user-details-name {{ user.username }}
        .dropdown-divider
        a.dropdown-item(routerLink='/dashboard')
            | Ajustes
        a.dropdown-item(routerLink='/dashboard')
            | Auditoría
        .dropdown-divider
        a.dropdown-item(routerLink='/auth/login')
            | Cerrar Sessión